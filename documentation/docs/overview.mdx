---
sidebar_position: 1
title: Overview
description: What the Cloud APIM Security Suite adds to Otoroshi, and how its pieces fit together.
---

# Overview

The Cloud APIM Security Suite is an [Otoroshi](https://www.otoroshi.io/) admin extension that adds
threat protection to the gateway: a **web application firewall** speaking ModSecurity SecLang with
the OWASP Core Rule Set embedded, **ip reputation** fed by threat intelligence feeds, CrowdSec and
the public routing table, **bot and AI-crawler control** with an embedded challenge — and a
**decision fabric** that makes those detectors act as one instead of four.

It runs entirely on the JVM, inside the Otoroshi process. There is no native library to install, no
sidecar, and no second daemon to operate.

## Why it sits in the gateway

A standalone WAF sees bytes. This one sits inside an API gateway, which means it already knows the
route, the consumer, the apikey and the backend response — and it reuses Otoroshi's entities,
admin API, analytics exporters and vault references instead of inventing its own.

## The decision fabric

Detectors do not each decide on their own. They contribute **signals** to a shared score, and one
component turns that score into one graded action — `log`, `tarpit`, `challenge`, `deny` or `ban` —
with cluster-wide bans, a cross-request memory and incident correlation behind it.

That arrangement is what makes the rest deployable: a false positive costs a log line at score 45
instead of a broken customer at 403. It is also why there is a single **preset plugin**: the chain
is position-sensitive, and the preset makes the order structural rather than documented.

→ [How the fabric works](./fabric/overview.mdx) · [The preset plugin](./fabric/preset.mdx)

## The four detection modules

<div class="row">
  <div class="col col--6">

### WAF

A JVM implementation of the [ModSecurity SecLang](https://github.com/owasp-modsecurity/ModSecurity/wiki/Reference-Manual-%28v3.x%29)
rule language, with the [OWASP CRS](https://coreruleset.org/) v4 bundled. It inspects the request
line, headers, cookies, arguments and body, and optionally the backend response.

The engine passes **100% of the CRS v4 regression suite**.

→ [WAF configurations](./waf/configs.mdx)

  </div>
  <div class="col col--6">

### IP reputation

Scores the caller before the request is parsed, from three kinds of source: **threat feeds** (public
blocklists, cloud provider ranges, Tor exit nodes — fourteen curated ones in the catalog),
**CrowdSec** decisions streamed from a Local API, and **ASN classification** from the public routing
table.

Lookups hit a sorted in-memory range index; nothing blocking happens on the request path.

→ [How reputation works](./reputation/how-it-works.mdx)

  </div>
</div>

<div class="row">
  <div class="col col--6">

### Bots and challenges

Twenty-seven known crawler signatures, **verified** by forward-confirmed reverse DNS rather than
trusted — a forged `Googlebot` becomes a demonstrated lie. Per-category rules over AI, search, SEO
and monitoring crawlers, with a matching `robots.txt` generated from them, plus **honeypot** paths
that catch a scanner with almost no false-positive risk.

The `challenge` action is served by an **embedded proof of work** — no third party, no external
call — or by a vendor widget, with European providers among the presets.

→ [Verified crawlers](./bots/crawlers.mdx) · [Challenges](./bots/challenge.mdx)

  </div>
  <div class="col col--6">

### Behaviour

Some attacks leave nothing a rule engine can see: credential stuffing is well-formed HTTP with a
wrong password. **Fail2ban** counts failed responses per caller and bans the repeat offenders —
counting in the shared store and banning through the cluster-wide registry, so the threshold means
the same thing on one node and on twelve.

→ [Fail2ban](./fabric/fail2ban.mdx)

  </div>
</div>

## Where each piece runs

| Stage | What runs | Cost |
|---|---|---|
| Before routing | Honeypots and the global validators — covers traffic matching no route at all | A path lookup |
| Access validation | Standing bans, bot verification, IP reputation, fail2ban's own bans | Node-local map reads and a binary search over merged ranges |
| Request | SecLang phases 1, 2 and 5 — URI, headers, cookies, args, body — then the threat response acts on the score | Proportional to the ruleset and the body size |
| Response | SecLang phases 3, 4 and 5 — status, response headers and body; failed statuses counted by fail2ban | Only when response inspection is enabled |

The cheap checks run first on purpose: a caller already known to be hostile is refused for the price
of a map lookup, without the rule engine ever seeing the request.

## What ships in the box

| | |
|---|---|
| **Entities** | Nine: `WafConfig`, `WafRuleset`, `ThreatPolicy`, `ThreatFeed`, `AsnDatabase`, `CrowdSecBouncer`, `BotPolicy`, `ChallengeProvider`, `HoneypotPolicy` — full CRUD, admin API, import/export, Kubernetes CRDs |
| **Route plugins** | WAF · IP reputation · Threat gate · Threat response · Bot guard · Fail2ban · [the preset](./fabric/preset.mdx) that lays them down in the right order |
| **Global plugins** | Three incoming-request validators — honeypot, WAF and IP reputation — evaluated before routing ([see the note](./reference/plugins.mdx#global-plugins-are-not-route-plugins)) |
| **Admin UI** | One *Cloud APIM - Security Suite* section, opening on an overview that says what to do next on this install: WAF configs, threat policies, threat feeds and their catalog, ASN databases, CrowdSec bouncers, bot policies, challenge providers and presets, honeypots, and a *Bans & incidents* console with evidence, an allowlist and an incident state a team can work |
| **Analytics** | Four event types, routable through any Otoroshi data exporter — including one normalised ECS-shaped `CloudApimSecurityEvent` |
| **Console** | A [route posture view](./console.mdx) that needs nothing, plus twelve analytics queries and a seeded dashboard when the user-analytics exporter is configured |
| **Tuning** | A [false-positive assistant](./waf/assistant.mdx) that turns an observed match into an exclusion, having run it against your configuration first, and a [learning mode](./waf/learning.mdx) that measures a window and says what arming would cost |

## Design rules

These hold everywhere in the extension, and they explain most of the API shape:

- **Nothing blocking on the request path.** Feed refreshes, DNS lookups and CrowdSec syncs run on a
  scheduler. Lookups touch only in-memory structures.
- **A failed refresh never removes protection.** If a feed stops responding, the previous snapshot
  keeps serving and the error is surfaced in the UI — silently losing coverage is worse than a
  visible failure.
- **Every external dependency fails open**, and the fail-open decision is itself an event, so a
  degraded WAF is visible rather than discovered later.
- **Monitoring mode is the default posture.** Every module can score and report without denying, so
  you can measure the false-positive cost before arming anything.

## Next

- [Install](./install.mdx) — download the jar, enable the extension
- [What a complete deployment needs](./architecture.mdx) — a redis for shared state, a postgres for
  analytics, and which features are silently degraded without them
- [Quickstart](./quickstart.mdx) — a protected route in about ten minutes
- [Protecting a route, end to end](./tutorial.mdx) — the long version: every entity, in the order
  that works, and how to arm it without breaking traffic
- [Security console](./console.mdx) — reading the events back, and which routes are actually protected
- [Tuning](./waf/tuning.mdx) — how to get from monitoring to blocking without breaking traffic
- [Tuning assistant](./waf/assistant.mdx) — the same work, from the admin UI, with every exclusion
  proven before it is saved
- [Learning mode](./waf/learning.mdx) — measure a window, then decide about arming with evidence
  instead of nerve
